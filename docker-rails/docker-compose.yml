version: '2'
services:
  web:
    build: .
    command: bundle exec rails s -p 3000 -b 0.0.0.0
    volumes:
      - .:/app
    volumes_from:
      - bundle
    ports:
      - 3000:3000
    depends_on:
      - db
    container_name: web
    stdin_open: true
    tty: true
  db:
    image: postgres
  bundle:
    image: busybox
    volumes:
      - /bundle
